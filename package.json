{
  "scripts": {
    "test": "tap --ts",
    "coverage": "mkdir -p coverage && tap --ts --coverage-report=text-lcov > coverage/lcov.info",
    "prettier": "prettier --write '**/**.ts'"
  },
  "devDependencies": {
    "@types/node": "^15.0.1",
    "@types/tap": "^15.0.0",
    "prettier": "^2.2.1",
    "tap": "^15.0.5",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
